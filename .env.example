# Account credentials
PRIMARY_BLUESKY_HANDLE=your-account.bsky.social
PRIMARY_BLUESKY_PASSWORD=your-password
SECONDARY_ACCOUNTS=account1.bsky.social,password1;account2.bsky.social,password2

# Moderation list settings
MOD_LIST_NAME=Synchronized Blocks
MOD_LIST_DESCRIPTION=This list contains accounts that are blocked by any of our managed accounts

# Database configuration (individual parameters)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=symm_blocks
DB_USER=postgres
DB_PASSWORD=your-password

# OR use a connection string (Railway style)
# DATABASE_URL=postgresql://username:password@host:port/database

# API URLs
CLEARSKY_API_URL=https://api.clearsky.services/api/v1/anon

# Logging
LOG_LEVEL=INFO

# Sync intervals
SYNC_INTERVAL_PRIMARY_MINUTES=15
SYNC_INTERVAL_SECONDARY_MINUTES=60
FULL_SYNC_INTERVAL_HOURS=24

# Test account for pagination testing
TEST_PAGINATION_BLUESKY_HANDLE=symm.social
TEST_PAGINATION_BLUESKY_PASSWORD=your-password 